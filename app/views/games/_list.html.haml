%ul.games
- @games.each do |game|
  %li
    = link_to(game.name, game_path(game))
    = "#{game.players.length} players" unless game.finished?
    = "Won by #{game.winner_type.pluralize}" if game.finished?
